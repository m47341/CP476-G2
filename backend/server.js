// main entry point to start app and link files together

function startServer() {
  // goal: starts backend server so it can listen for incoming traffic
  // sets app to listen on a specific port (eg, 3000) and logs a message when running
}

function configureRoutes() {
  // goal: connect all route files to main server app
  // links auth, patron, and admin route files so app recognizes them
}
